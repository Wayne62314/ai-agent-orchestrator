# 阶段十第三部分实施报告：升级迁移与卸载数据保护

## 结论

阶段十第三部分把“升级和卸载不能破坏用户数据”从文档约定变成了可执行控制。
数据库不再直接在正式文件上逐段迁移，而是先生成经完整性检查的备份及 SHA-256
清单，再在临时副本中完成全部向前迁移。只有版本历史、数据库完整性和外键检查
全部通过，临时副本才会原子替换正式数据库。

Windows CI 也不再止于“安装包能编译”：它会真实执行安装、检查已安装程序与
快捷方式、运行冻结 sidecar 自检、执行卸载并验证用户数据保留，再独立验证
`/AUTOSTART` 和卸载清理。

## 数据库升级控制

### 备份优先

已有数据库需要升级时，应用在 `backups/pre-upgrade` 中创建：

- `pre-upgrade-v<旧>-to-v<新>-<时间>.db`；
- 同名 `.manifest.json`。

清单只包含文件名、大小、SHA-256、源 Schema、目标 Schema 和 UTC 创建时间，
不包含绝对路径、任务正文、凭据或环境变量。每一条升级路径保留最近五份备份，
数据库与清单按对清理。

### 临时副本和失败即停

升级顺序为：

1. 验证正式数据库 `integrity_check`；
2. 校验迁移历史从 1 开始连续且不超过当前程序支持版本；
3. 创建并再次验证 SQLite 一致性备份；
4. 用 SQLite backup API 建立私有迁移副本；
5. 只在副本中执行全部待应用的向前迁移；
6. 检查目标版本历史、`integrity_check` 和 `foreign_key_check`；
7. 原子替换正式数据库。

任一迁移语句、完整性检查或版本检查失败时，临时副本被丢弃，正式数据库保持
原样，应用启动失败并等待用户恢复或升级程序。旧程序遇到更高 Schema 时也会
明确拒绝打开，不会猜测兼容性。

### Schema v7

本部分新增 Schema v7 的 `app_settings` 表，作为后续持久化非敏感桌面设置的
稳定位置。它不用于保存 Codex 凭据、令牌或原始认证记录。桌面安装版本同步提升
到 `0.11.0`，使安装器能够按正常覆盖升级语义识别本次变化。

## Windows 安装与卸载矩阵

`windows-latest` CI 对正式收集后的安装器执行两轮测试：

| 场景 | 验证 |
| --- | --- |
| 默认静默安装 | 主程序、冻结 sidecar、卸载器、开始菜单和桌面快捷方式存在 |
| 已安装运行时 | 冻结 sidecar `--self-check` 返回健康，不使用开发态 Python 入口 |
| 默认登录启动 | HKCU `Run` 项不存在 |
| 默认卸载 | 程序移除，Roaming/Local AppData 哨兵文件均保留 |
| 显式 `/AUTOSTART` | HKCU `Run` 指向已安装主程序 |
| 启用后卸载 | 登录启动项被移除，用户数据仍保留 |

测试使用随机的 `%TEMP%` 安装目录，避免依赖或污染固定程序目录。正式数据删除
选项仍需用户在交互式卸载器中明确勾选；CI 不自动触发该破坏性路径。

## 自动验证

- 旧 Schema 升级会创建可校验备份和清单；
- 同一版本重复启动不会重复备份；
- 人为注入的失败迁移不会改变正式数据库的哈希、表或版本；
- 缺失迁移序号的数据库会停止启动；
- 新 Schema 数据库会被旧程序拒绝且不会创建或修改文件；
- 现有备份、恢复、任务重启和完整测试套件继续通过；
- PowerShell 安装矩阵已接入正式 Windows CI 门禁。

## 尚未完成

本部分的 CI runner 不是完全空白的最终用户虚拟机，也没有交互完成完整 UI
首次启动。因此阶段十仍需第四部分：

1. 在干净 Windows 10 22H2 与 Windows 11 VM 完成首次启动和 Codex 登录；
2. 使用真实 `0.10.0 → 0.11.0` 安装器覆盖升级并核对任务、Worktree 与备份；
3. 验证显式“同时删除数据”路径只删除批准目录；
4. 对候选 `Setup.exe` 执行 Defender/多引擎恶意软件扫描；
5. 核对 SHA-256 来源并形成阶段十候选版本验收记录。
