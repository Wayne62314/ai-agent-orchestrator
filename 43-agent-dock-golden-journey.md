# Agent Dock 最小黄金旅程

## 目标

验证产品最关键的使用感受：用户在 Agent Dock 中管理任务，而官方 Codex
像可插拔工作窗口一样位于中央。Agent Dock 不复制、不重做 Codex 界面。

## 黄金旅程

1. 用户打开现有可安装产品。
2. 左侧直接显示本机任务，并可新建任务。
3. 新建任务既支持现有 Git 仓库，也支持新建项目目录和本地仓库。
4. 产品为每个任务创建并持久绑定一个真实 Codex thread。
5. 选择左侧任务时，通过 `codex://threads/<thread-id>` 打开对应官方任务。
6. 用户把官方 Codex 窗口拖近中央插槽。
7. 插槽显示半透明磁吸预览；释放鼠标后，真实 Codex 窗口附着到中央。
8. 切换左侧任务时，已附着的 Codex 跟随切换到对应任务。
9. 右侧消息队列只展示真正需要用户处理的审批或异常。

## 复用范围

- 保留 Tauri 安装器、Python 本地后台、SQLite 数据、账户、Git Worktree、
  任务生命周期、自动验收、Checkpoint、审批与维护能力。
- 复用原有新建任务流程，包括“打开现有项目”和“创建新项目”。
- 复用 Windows 磁吸技术验证得到的窗口识别、父子窗口附着、恢复和位置同步结论。
- 重做正式产品的信息架构：左侧任务、中央官方 Codex、右侧消息。

## 安全与产品边界

- 只识别路径属于官方 OpenAI Codex 安装位置的可见窗口。
- 附着时不读取 Codex 内容、账户信息或输入内容。
- 离开工作台或退出产品时恢复 Codex 为独立窗口。
- 每个产品任务只创建一个持久 Codex thread；开始执行时继续使用同一 thread。
- 项目命令仍为可选；AI 复核保留，且不冒充独立测试证据。

## 当前验收

- 前端黄金旅程测试覆盖三栏工作台、任务切换、新项目、可选验收、失败反馈和真实审批。
- 中央下方任务托盘直接提供启动、暂停、恢复、取消和最终确认，不需要离开已附着的 Codex。
- 右侧消息队列只显示需要用户处理的后台异常、审批和需关注任务，并可在当前工作台内完成审批。
- 任务动作先更新当前界面再从后台校准，避免用户点击后长时间没有反馈。
- Python 后台全部 171 项测试通过。
- 正式前端生产构建通过。
- Windows 原生宿主层需要在带 MSVC Build Tools 的 CI 中完成编译和安装包实机验证。
