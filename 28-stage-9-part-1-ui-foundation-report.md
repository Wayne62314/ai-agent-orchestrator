# 阶段九第一部分：桌面 UI 基础与私有 RPC

## 1. 结论

阶段九已启动。第一部分交付了可在浏览器中运行和自动测试的桌面界面基础，
同时建立了桌面外壳到 Python 应用服务的私有 JSONL RPC 读取边界。

这不是最终安装包。当前重点是先固定普通用户可理解的界面、交互和安全边界，
再在后续 PR 中接入 Tauri 原生窗口、真实写操作和 Windows 通知。

## 2. 本次交付

- React 19、TypeScript、Vite 和 Vitest 桌面前端；
- 首次启动引导、首页、任务详情、任务向导、审批中心和设置；
- `RUNNING`、`PAUSED`、`WAITING_FOR_APPROVAL` 等稳定状态的人类可读标签；
- 暂停、恢复、取消和审批交互的 Fake Adapter；
- `aiao.desktop.v1` UTF-8 JSONL RPC；
- 1 MiB 请求上限、方法白名单、参数校验和有界分页；
- 从 SQLite 重建任务、审计、验收与审批视图；
- 不向桌面 RPC 暴露直接 SQL 写入或任意命令执行；
- 前端测试与生产构建加入 CI。

## 3. 安全边界

React 只调用一个受控桌面桥接函数。开发模式使用内存 Fake Adapter；
Tauri 模式调用 `sidecar_request`。Python RPC 第一部分只提供查询方法，
所有未来状态变更仍必须经过 `OrchestratorService.process_event`、审批和审计。

协议拒绝：

- 未知版本；
- 未知方法；
- 非对象参数；
- 超长消息；
- 越界分页。

原始标准输出、标准错误、凭据和认证记录不会写入桌面快照。

## 4. 验证证据

- 桌面用户旅程：3 项通过；
- 桌面生产构建：通过；
- RPC 边界测试：5 项通过；
- 完整 Python 回归测试：125 项通过；
- Ruff：通过；
- Python sdist 与 wheel：构建通过；
- 原生窗口视觉走查留在 Tauri 外壳接入部分完成。

## 5. 尚未完成

以下属于阶段九后续部分：

1. Tauri 2 原生窗口和 Rust 命令白名单；
2. 打包并监管 Python sidecar；
3. 真实创建、暂停、恢复、取消与审批应用服务；
4. Codex 登录、仓库选择器和文件夹权限；
5. 备份恢复、诊断导出和 Windows 本地通知；
6. 真实 Codex 完整桌面端到端验收。

阶段九全部退出标准尚未达成，阶段十安装包工作尚未开始。
