# 01 — 产品简述

## 1. 问题

长时间 AI 开发任务经常被以下情况打断：

- 模型额度或速率限制；
- 会话上下文丢失或压缩；
- 本地进程、网络或机器中断；
- 等待 CI、PR 评论或外部依赖；
- 高风险步骤需要人工批准。

目前用户通常要亲自记录进度、等待条件恢复、重新解释上下文并监督验证。这使 AI Agent 仍然依赖“人值守”。

## 2. 产品主张

AI Agent Orchestrator 将一次开发任务变成一个可持久化、可暂停、可恢复、可验收的工作流。

它的职责不是替代编码 Agent，而是：

- 保存目标、约束、计划与当前进度；
- 为每个暂停点生成结构化检查点；
- 监听可验证的恢复事件；
- 构造最小而充分的恢复上下文；
- 触发执行引擎继续工作；
- 运行验收并决定继续、重试、等待或升级给人。

## 3. 目标用户

首要用户：

- 经常让 Codex 等 Agent 处理数小时开发任务的个人开发者；
- 同时管理多个代码任务、但不想持续守候的技术负责人；
- 希望 AI 产出具备测试和审计证据的小团队。

暂不优先：

- 需要企业级多租户管理的大型组织；
- 完全无人审批的生产运维；
- 非开发类通用个人助理场景。

## 4. 核心使用场景

### 场景 A：额度或服务暂不可用

Agent 工作到一半遇到限制。编排器记录检查点，等待恢复信号，随后发送带完整状态的恢复请求。

### 场景 B：等待 CI

Agent 提交代码后进入等待。CI 完成时，编排器根据结果继续下一步或把失败信息交给 Agent 修复。

### 场景 C：等待人工批准

Agent 准备执行发布、合并或破坏性操作。编排器暂停任务，展示风险、变更和验证证据；只有获得授权才继续。

### 场景 D：会话重建

即使原会话不可恢复，新执行会话也能依靠检查点和仓库事实继续任务。

## 5. 差异化价值

价值不在“自动过几个小时再发一句继续”，而在以下组合：

- 以持久状态取代脆弱的聊天记忆；
- 以条件和事件取代单一定时器；
- 以验收证据取代 Agent 自我声明完成；
- 以权限边界和审批门槛支持安全的长时间运行；
- 将具体模型或工具封装为可替换执行引擎。

## 6. 成功指标

MVP 建议记录：

- **恢复成功率**：暂停后无需用户重复解释即可继续的任务比例；
- **检查点完整率**：恢复所需字段齐全的检查点比例；
- **无人值守时长**：任务在无需用户介入下推进的时间；
- **误触发率**：条件未满足却恢复执行的比例；
- **重复副作用率**：重试造成重复提交、消息或外部操作的比例；
- **验证通过率**：标记完成前实际通过约定验收的任务比例；
- **人工介入质量**：升级给用户时是否包含足够决策信息。

## 7. 非目标

MVP 不承诺：

- 绕过任何产品额度、使用限制或平台保护；
- 模拟用户规避平台规则；
- 完全自主地向生产环境发布；
- 自动合并所有 PR；
- 同时编排大量 Agent；
- 支持所有模型供应商；
- 用向量数据库长期保存所有聊天内容。

## 8. 产品边界

编排器只执行用户明确授权的任务和动作。暂停与恢复不会扩大原任务权限；恢复只是继续原有授权范围内的工作。

