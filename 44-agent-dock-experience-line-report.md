# Agent Dock 0.13.0 可体验主线报告

## 本轮目标

本轮只交付一条可连续体验、可判断产品方向的主线：用户在 Agent Dock 中创建或选择任务，使用与任务绑定的真实官方 Codex，对任务进行控制，并只在需要判断时处理右侧消息。

## 已交付体验

1. 左侧可创建任务并在已有任务之间切换。
2. 每个任务持久绑定一个官方 Codex thread；选择任务时打开对应 thread。
3. 用户把官方 Codex 窗口拖入中央磁吸区后，Codex 以独立真实窗口附着并保持可输入。
4. Codex 只在用户完成拖放时附着，不因恢复窗口或位置重合自动附着。
5. 切换左侧任务时，已附着的 Codex 跟随打开对应任务。
6. 中央下方任务托盘显示真实状态、进度和下一步，并提供启动、暂停、恢复、取消与最终确认。
7. 右侧消息队列显示真实后台异常、审批和需关注任务；审批可在当前工作台内完成，不会把 Codex 从中央移走。
8. Agent Dock 与 Codex 作为一组激活时，Codex 保持在 Dock 上方；切换到其他应用后不会全局置顶。

## 本轮明确不做

- 不复制或伪造 Codex 的聊天界面。
- 不读取官方 Codex 窗口内的对话、账户或输入内容。
- 不加入多执行器、团队看板或复杂数据分析。
- 不向用户提供额外的验收脚本；安装后直接按产品主线体验。

## 自动验证

- 桌面界面测试：9 项。
- Python 后台测试：171 项。
- 前端生产构建、Rust 格式与依赖解析。
- Windows 安装、覆盖升级、卸载数据保护与 Defender 扫描由 CI 生成证据。

## 实机审核重点

1. 新建任务后是否立即出现在左侧并自动成为当前任务。
2. 官方 Codex 是否只能通过真实拖放附着，附着后能否正常输入。
3. 点击 Agent Dock 后，Codex 是否仍在中央上层；切换到其他软件后是否不会遮挡其他软件。
4. 切换任务时是否打开正确的 Codex thread。
5. 启动、暂停、恢复、取消是否立即给出清晰状态反馈。
6. 右侧审批是否能完成，同时保持中央 Codex 不被移走。
