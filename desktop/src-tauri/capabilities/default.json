{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-main",
  "description": "Minimum core permissions for the local desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
