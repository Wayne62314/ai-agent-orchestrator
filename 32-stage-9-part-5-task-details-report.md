# 阶段九第五部分：任务详情与持久化证据

## 1. 结论

阶段九第五部分把任务详情页从静态占位内容升级为真实的本地证据浏览器。用户可以
在桌面界面中查看活动、Codex Run、Checkpoint、验收结果和交付结论，不需要使用
命令行或直接读取 SQLite。

所有详情都从持久化存储重新构建，经统一脱敏后通过私有 sidecar RPC 返回。窗口刷新
或进程重启不会依赖前端内存恢复记录。

## 2. 只读详情协议

新增 `task/detail` 方法，接受：

- `taskId`：任务标识；
- `section`：`activities`、`runs`、`checkpoints`、`verifications` 或
  `report`；
- `limit`：1–100 的整数；
- `cursor`：只能用于原标签的下一页游标。

活动、Run、Checkpoint 和 Verification 使用稳定的倒序游标分页。不同标签的游标
不能混用，错误类型、越界页大小和格式错误会被明确拒绝。

协议不会返回凭据、原始认证记录或原始 stdout/stderr。命令、摘要、错误和审计载荷
在进入桌面读模型前统一脱敏。

## 3. 桌面体验

任务详情页现在提供：

1. 活动：展示审计序号、事件含义、脱敏详情和时间；
2. 运行：展示执行轮次、引擎、状态、结果摘要和结束原因；
3. Checkpoint：展示序号、状态、工作区版本和内容摘要；
4. 验收：展示轮次、必选性、状态、耗时、退出码和命令；
5. 报告：展示审计链有效性、每轮验收汇总和当前或最终交付结论。

每个标签按需读取，可以手动刷新和加载更早记录。空状态、加载状态和读取错误都有
独立引导；读取失败不会清除已经显示的数据。

详情请求带有标签归属保护。用户快速切换标签时，过期响应不能进入新标签，避免把
活动数据误当作 Run 或验收数据渲染。

任务版本在后台变化或审批完成后，当前详情标签会从持久化存储重新加载。

## 4. 安全边界

- UI 仍不直接访问或修改数据库；
- 分页接口只暴露稳定、受限的读模型；
- 游标不接受跨标签重放；
- 报告从持久化任务、验收和审计链即时重建；
- Checkpoint 只展示标识、状态和摘要，不暴露载荷文件；
- Verification 只展示脱敏摘要，不把原始日志内容放入 RPC。

## 5. 验收

- 后端详情接口覆盖持久化读取、分页、游标隔离和脱敏；
- 桌面自动化覆盖活动、Run、Checkpoint、验收和报告五个标签；
- 快速标签切换竞态有自动化回归保护；
- Python 全量测试、Ruff、前端测试和正式构建必须全部通过；
- Windows 原生外壳由 GitHub Actions 继续完成跨平台编译验证。

## 6. 下一部分

阶段九第六部分完成桌面 UI 收尾：

1. 真实备份、恢复和诊断导出入口；
2. Windows 本地通知；
3. 设置、危险操作确认和恢复引导；
4. 可访问性、响应式布局和完整无终端用户旅程；
5. 完成阶段九退出标准，进入 Windows 安装与打包。
