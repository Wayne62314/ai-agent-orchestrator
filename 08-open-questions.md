# 08 — 未决问题与决策记录

## 1. 必须先回答

### Q1：首个执行引擎是什么？

候选：

- Codex 提供的官方可自动化入口；
- 其他具备稳定 API 或 CLI 的编码 Agent；
- 一个用于验证编排逻辑的假执行器。

决策标准：

- 合规；
- 可传入上下文；
- 可观察运行状态；
- 可取消；
- 输出足够结构化；
- 不依赖脆弱的 UI 自动化。

决定：Codex。任务执行优先使用官方 Python Codex SDK；限额观察使用 App Server JSON-RPC。详情见 [10-execution-adapter-decision.md](./10-execution-adapter-decision.md)。

### Q2：如何判断“额度恢复”？

优先顺序：

1. 官方明确提供的状态或错误响应；
2. 官方建议的重试时间或退避信息；
3. 用户设置的恢复时间；
4. 低频、受限的能力探测；
5. 人工恢复。

在验证前，不应声称可以精确监控剩余 token 或额度恢复时刻。

验证结果：使用 ChatGPT 管理的 Codex 认证时，App Server 的 `account/rateLimits/read` 能返回限额窗口、使用百分比和 `resetsAt`，并提供 `account/rateLimits/updated` 通知。该能力与认证类型和服务返回数据有关，因此仍保留定时回查和人工恢复作为降级路径。

### Q3：首版运行在哪里？

默认假设：用户本机后台进程。

原因：

- 可以直接访问本地仓库；
- 权限边界更直观；
- 无需先解决云端凭据和代码上传；
- 更适合验证单用户闭环。

### Q4：自动提交是否属于 MVP？

建议：支持但默认 `ask`。

自动提交能形成清晰检查点，但也会改变版本历史。第一版可先完成文件修改和验收，审批后再提交。

## 2. 重要但可稍后决定

- 编程语言和框架；
- SQLite 迁移策略；
- 是否提供本地 Web UI；
- 检查点采用 YAML 还是 JSON；
- 如何压缩大型测试日志；
- 多仓库任务的边界；
- Git worktree 隔离；
- 多执行引擎路由；
- 通知渠道；
- 商业模式。

## 3. 当前假设

| 编号 | 假设 | 风险 | 验证方式 |
| --- | --- | --- | --- |
| H1 | 用户愿意预先写验收命令 | 中 | 用 5 个真实任务测试配置成本 |
| H2 | 结构化检查点足以支持新会话接手 | 高 | 阶段 0 恢复实验 |
| H3 | 单机 SQLite 足以支撑 MVP | 低 | 并发和崩溃测试 |
| H4 | 执行引擎能返回可辨认的退出原因 | 高 | 适配器探索 |
| H5 | 不读取精确额度也能实现有价值的恢复 | 中 | 定时/人工/探测三种策略对比 |
| H6 | 用户接受高风险动作需审批 | 低 | 原型访谈 |

## 4. 决策记录

### D-001：MVP 聚焦单任务、单机

- 状态：已采纳
- 原因：先证明恢复闭环，避免调度复杂性掩盖核心价值。

### D-002：额度监控不是架构核心

- 状态：已采纳
- 原因：额度信息是否可得由平台决定；系统应围绕通用恢复信号设计。

### D-003：成功必须经过外部验收

- 状态：已采纳
- 原因：Agent 的完成声明不是可靠证据。

### D-004：SQLite 作为 MVP 默认状态存储

- 状态：已采纳并通过阶段 1 验证
- 原因：需要事务、去重和崩溃恢复，同时保持本地部署简单。

### D-005：首个执行引擎采用 Codex

- 状态：已采纳
- 原因：官方 SDK 和 App Server 已覆盖线程启动、恢复、完成事件、中断与限额观察。

### D-006：采用 SDK + App Server 薄适配层

- 状态：已采纳
- 原因：Python SDK 提供固定版本运行时和稳定的线程 API；当前版本的高层 API 尚未暴露限额读取，因此用一个受版本约束的 JSON-RPC 适配层补齐。

### D-007：桌面应用调度不是状态真相源

- 状态：已采纳
- 原因：桌面定时任务适合唤醒和跟进，但编排器仍需自行持久化状态、事件去重和验收证据。

### D-008：Run 使用可过期租约

- 状态：已采纳并通过阶段 2 验证
- 原因：外部 Codex turn 与 SQLite 无法构成同一事务；租约使崩溃后的未完成运行可被识别和安全放弃。

### D-009：Checkpoint 使用两阶段文件提交

- 状态：已采纳并通过阶段 2 验证
- 原因：先登记 `PENDING`，再原子替换文件并标记 `READY`，避免半写文件被用作恢复点。

### D-010：正常多阶段工作使用 `CONTINUATION_REQUIRED`

- 状态：已采纳
- 原因：阶段完成后继续下一阶段不是验收失败，不能滥用 `CHECKS_FAILED_RETRYABLE`。

### D-011：真实执行默认只读

- 状态：已采纳
- 原因：read-only 足以验证控制链；workspace-write 必须由任务调用方显式开启。

### D-012：高风险动作默认询问，未知动作默认拒绝

- 状态：已采纳并通过阶段 4 验证
- 原因：恢复不能扩大授权；高风险动作必须让用户看到风险、回滚和精确参数，未识别动作不能推断为安全。

### D-013：审批绑定规范化动作哈希

- 状态：已采纳并通过 `AC-05`
- 原因：只按动作名称审批会允许参数漂移；绑定动作类型、逻辑步骤和规范化参数可使任何变化自动失效。

### D-014：不确定副作用不自动重放

- 状态：已采纳并通过阶段 4 验证
- 原因：外部请求和本地落库无法构成同一事务；结果不明时保守进入 `UNKNOWN`，必须先对账。

## 5. 下一次方案评审建议

按以下顺序决定：

1. 选定首个执行引擎和合法接入方式；
2. 用一次人工实验验证 Resume Package；
3. 决定 MVP 是否包含自动提交；
4. 决定定时器之外的第一个外部事件；
5. 再选择语言和开始原型实现。
