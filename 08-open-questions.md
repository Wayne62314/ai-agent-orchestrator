# 08 — 未决问题与决策记录

## 1. 必须先回答

### Q1：首个执行引擎是什么？

候选：

- Codex 提供的官方可自动化入口；
- 其他具备稳定 API 或 CLI 的编码 Agent；
- 一个用于验证编排逻辑的假执行器。

决策标准：

- 合规；
- 可传入上下文；
- 可观察运行状态；
- 可取消；
- 输出足够结构化；
- 不依赖脆弱的 UI 自动化。

决定：Codex。任务执行优先使用官方 Python Codex SDK；限额观察使用 App Server JSON-RPC。详情见 [10-execution-adapter-decision.md](./10-execution-adapter-decision.md)。

### Q2：如何判断“额度恢复”？

优先顺序：

1. 官方明确提供的状态或错误响应；
2. 官方建议的重试时间或退避信息；
3. 用户设置的恢复时间；
4. 低频、受限的能力探测；
5. 人工恢复。

在验证前，不应声称可以精确监控剩余 token 或额度恢复时刻。

验证结果：使用 ChatGPT 管理的 Codex 认证时，App Server 的 `account/rateLimits/read` 能返回限额窗口、使用百分比和 `resetsAt`，并提供 `account/rateLimits/updated` 通知。该能力与认证类型和服务返回数据有关，因此仍保留定时回查和人工恢复作为降级路径。

### Q3：首版运行在哪里？

默认假设：用户本机后台进程。

原因：

- 可以直接访问本地仓库；
- 权限边界更直观；
- 无需先解决云端凭据和代码上传；
- 更适合验证单用户闭环。

### Q4：自动提交是否属于 MVP？

建议：支持但默认 `ask`。

自动提交能形成清晰检查点，但也会改变版本历史。第一版可先完成文件修改和验收，审批后再提交。

## 2. 重要但可稍后决定

- 大型日志压缩策略（延期到 v1.1）；
- 多仓库任务边界（延期到 v1.1）；
- 多执行引擎路由（延期到 v2.0 后）；
- 外部通知渠道（延期到 v1.2）；
- 商业模式（v1.0 不涉及）。

以下旧未决项已经关闭：

- 后端语言：Python；
- 状态存储与迁移：SQLite 前向迁移；
- Checkpoint：JSON；
- 用户界面：Windows 桌面应用窗口 + 本地后台服务；
- Git worktree：纳入 v1.0 任务隔离；
- 本地通知：纳入 v1.0。

## 3. 当前假设

| 编号 | 假设 | 风险 | 验证方式 |
| --- | --- | --- | --- |
| H1 | 用户愿意预先写验收命令 | 中 | 用 5 个真实任务测试配置成本 |
| H2 | 结构化检查点足以支持新会话接手 | 高 | 阶段 0 恢复实验 |
| H3 | 单机 SQLite 足以支撑 MVP | 低 | 并发和崩溃测试 |
| H4 | 执行引擎能返回可辨认的退出原因 | 高 | 适配器探索 |
| H5 | 不读取精确额度也能实现有价值的恢复 | 中 | 定时/人工/探测三种策略对比 |
| H6 | 用户接受高风险动作需审批 | 低 | 原型访谈 |

## 4. 决策记录

### D-001：MVP 聚焦单任务、单机

- 状态：已采纳
- 原因：先证明恢复闭环，避免调度复杂性掩盖核心价值。

### D-002：额度监控不是架构核心

- 状态：已采纳
- 原因：额度信息是否可得由平台决定；系统应围绕通用恢复信号设计。

### D-003：成功必须经过外部验收

- 状态：已采纳
- 原因：Agent 的完成声明不是可靠证据。

### D-004：SQLite 作为 MVP 默认状态存储

- 状态：已采纳并通过阶段 1 验证
- 原因：需要事务、去重和崩溃恢复，同时保持本地部署简单。

### D-005：首个执行引擎采用 Codex

- 状态：已采纳
- 原因：官方 SDK 和 App Server 已覆盖线程启动、恢复、完成事件、中断与限额观察。

### D-006：采用 SDK + App Server 薄适配层

- 状态：已采纳
- 原因：Python SDK 提供固定版本运行时和稳定的线程 API；当前版本的高层 API 尚未暴露限额读取，因此用一个受版本约束的 JSON-RPC 适配层补齐。

### D-007：桌面应用调度不是状态真相源

- 状态：已采纳
- 原因：桌面定时任务适合唤醒和跟进，但编排器仍需自行持久化状态、事件去重和验收证据。

### D-008：Run 使用可过期租约

- 状态：已采纳并通过阶段 2 验证
- 原因：外部 Codex turn 与 SQLite 无法构成同一事务；租约使崩溃后的未完成运行可被识别和安全放弃。

### D-009：Checkpoint 使用两阶段文件提交

- 状态：已采纳并通过阶段 2 验证
- 原因：先登记 `PENDING`，再原子替换文件并标记 `READY`，避免半写文件被用作恢复点。

### D-010：正常多阶段工作使用 `CONTINUATION_REQUIRED`

- 状态：已采纳
- 原因：阶段完成后继续下一阶段不是验收失败，不能滥用 `CHECKS_FAILED_RETRYABLE`。

### D-011：真实执行默认只读

- 状态：已采纳
- 原因：read-only 足以验证控制链；workspace-write 必须由任务调用方显式开启。

### D-012：高风险动作默认询问，未知动作默认拒绝

- 状态：已采纳并通过阶段 4 验证
- 原因：恢复不能扩大授权；高风险动作必须让用户看到风险、回滚和精确参数，未识别动作不能推断为安全。

### D-013：审批绑定规范化动作哈希

- 状态：已采纳并通过 `AC-05`
- 原因：只按动作名称审批会允许参数漂移；绑定动作类型、逻辑步骤和规范化参数可使任何变化自动失效。

### D-014：不确定副作用不自动重放

- 状态：已采纳并通过阶段 4 验证
- 原因：外部请求和本地落库无法构成同一事务；结果不明时保守进入 `UNKNOWN`，必须先对账。

### D-015：外部内容与恢复条件分离

- 状态：已采纳并通过阶段 5 验证
- 原因：webhook 签名只能证明来源，不能证明评论正文是可信指令。恢复条件只允许适配器提取的白名单元数据；PR 评论正文不持久化、不参与条件匹配，也不改变权限。

### D-016：最终交付物是可安装 Windows 桌面产品

- 状态：已批准
- 原因：命令行技术 MVP 不是最终用户体验；UI、安装、升级、卸载和真实 Codex 主链属于产品完成条件。

### D-017：桌面窗口配合本地后台服务

- 状态：已批准
- 原因：保留现有 Python 编排内核和本地数据边界，同时提供正常桌面应用体验。

### D-018：v1.0 使用手动更新

- 状态：已批准
- 原因：先证明升级和数据迁移可靠，再在 v1.1 引入自动更新。

### D-019：v1.0 默认不开放公网

- 状态：已批准
- 原因：本地单用户产品不应为了 Webhook 默认增加隧道、TLS 和公网攻击面。

### D-020：自动提交支持但默认审批

- 状态：已批准
- 原因：提交有助于形成恢复点，但会改变版本历史，必须由用户控制。

### D-021：登录后启动为可选且默认关闭

- 状态：已批准
- 原因：安装不应静默增加后台常驻行为。

### D-022：v1.0 不收集遥测

- 状态：已批准
- 原因：本地代码与任务数据敏感；在没有明确隐私设计和用户同意前不采集。

### D-023：卸载默认保留用户数据

- 状态：已批准
- 原因：任务状态、审计和备份不可轻易恢复，删除必须由用户显式选择。

### D-024：v1.0 保持单用户、单机和单活动任务

- 状态：已批准
- 原因：先把一个真实任务的完整桌面体验做可靠，再扩大调度范围。

### D-025：采用 Tauri、React 和 Python sidecar

- 状态：已批准
- 原因：保留 Python 领域内核，同时获得现代 Windows UI、系统集成和安装路径。

### D-026：托盘应用拥有后台进程

- 状态：已批准
- 原因：关闭窗口不应中断长任务；v1 不需要难以察觉的 Windows Service。

### D-027：桌面主链使用私有 stdio RPC

- 状态：已批准
- 原因：不开放本地端口即可消除 CSRF、端口冲突和会话令牌攻击面。

### D-028：Codex 拥有登录和凭据

- 状态：已批准
- 原因：使用 App Server 稳定账户接口和 Windows keyring，不自建 OAuth 或保存令牌。

### D-029：Task 独占保留型 Git Worktree

- 状态：已批准
- 原因：Agent 不接触原工作目录；提交需审批，合并、推送和删除不自动执行。

### D-030：程序、数据和用户可见备份分离

- 状态：已批准
- 原因：升级只替换程序，卸载默认保留数据，备份可由用户直接找到。

### D-031：v1 发布每用户 x64 NSIS Setup.exe

- 状态：已批准
- 原因：覆盖首批 Windows 用户并降低权限与发布矩阵复杂度，迁移前强制备份。

## 5. 下一次方案评审

阶段 7 已完成，阶段 8 按以下顺序实施和评审：

1. `PAUSED` 状态、暂停/恢复事件和向前迁移；
2. 产品级 Codex App Server 会话与登录服务；
3. Task 启动、暂停、恢复、取消和单活动 Task 互斥；
4. Worktree 创建、校验和安全清理；
5. Run 租约、Checkpoint、额度等待、Verification 和报告闭环；
6. 使用真实 Codex 进行端到端验收。

批准的完整产品化范围见 [23-productization-roadmap.md](./23-productization-roadmap.md)。
