# 阶段 6 第六部分：本地优先部署

## 1. 决策

当前项目采用 Windows 本地部署，不继续创建 Railway 或其他云平台资源。PR #9 的合并记录保留，但其代码通过新的反向提交撤销，不重写 `main` 历史。

本地方案不需要：

- 云服务器；
- Railway 账号；
- Docker；
- 公网域名；
- 持续付费资源。

## 2. 运行模型

```text
Windows 当前用户
  └─ PowerShell 启动脚本
      └─ 独立 Python 虚拟环境
          └─ agent-orchestrator
              ├─ 127.0.0.1:8080
              ├─ SQLite: .local-runtime/data/state.db
              ├─ Worker
              └─ 本地日志
```

服务只绑定 loopback，不开放局域网或公网端口。电脑关机、睡眠或用户未登录时，任务不会继续运行。

## 3. 生命周期

提供以下 Windows 脚本：

- `install.ps1`：创建虚拟环境、安装项目、初始化数据库和生成本地密钥；
- `run.ps1`：前台运行服务并维护 PID 文件；
- `start.ps1`：通过计划任务或隐藏窗口启动并等待就绪；
- `status.ps1`：检查进程、`healthz` 和 `readyz`；
- `stop.ps1`：停止计划任务实例和本地进程；
- `backup.ps1`：执行在线一致性 SQLite 备份；
- `restore.ps1`：在服务停止后进行受保护恢复。

`install.ps1 -RegisterStartup` 会注册两个当前用户、非管理员计划任务：

- 登录后启动服务；
- 每天 03:00 创建备份。

## 4. 数据保护

备份使用 SQLite Backup API，不直接复制正在写入的数据库文件。每份备份都执行完整性检查，并通过同目录临时文件原子落盘。

恢复具备三道保护：

1. 必须提供 `-ConfirmReplace`；
2. 检测到本地服务仍在运行时拒绝恢复；
3. 替换前自动保存当前数据库。

默认只保留最近 30 份备份。磁盘级灾难仍需要用户把备份目录复制到另一块磁盘。

## 5. Webhook 边界

localhost 无法直接接收 GitHub 的公网 Webhook。当前本地部署保留 webhook HTTP 能力，但不自动创建隧道，也不修改防火墙。

这不影响本地持久状态、任务恢复、自动验收、审批和手动运维。若以后需要 GitHub 主动推送，可以把“固定安全隧道”作为独立、可撤销的可选功能重新评估。

## 6. 版本与发布

撤销 PR #9 后，包版本恢复为已发布的 `0.7.0`。本地部署脚本直接安装当前仓库，不需要发布新的 GHCR 镜像。

## 7. 下一步

本机已经完成：

- 创建独立虚拟环境并安装 `0.7.0`；
- 初始化本地数据库；
- 以隐藏窗口启动服务；
- 验证 `/healthz` 和 `/readyz`；
- 创建在线一致性备份；
- 停止服务并从备份恢复；
- 验证恢复前安全快照；
- 重新启动并再次通过健康检查。

完整测试共 104 项通过，Ruff、包构建、PowerShell 语法、数据库完整性和 `git diff --check` 均通过。

下一步只需审核并合并本地部署 PR。登录自启动和每日 03:00 备份保持可选，当前没有擅自注册系统计划任务。
