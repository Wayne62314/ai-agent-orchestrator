# 阶段九完成报告：桌面 UI 与本地维护闭环

## 1. 结论

阶段九已完成。普通用户可以在桌面界面中完成 Codex 登录、选择仓库、创建与控制
任务、处理审批、查看持久化证据，并执行备份、恢复和诊断导出。核心流程不再要求
终端或直接操作 SQLite。

桌面产品仍是本地优先架构：Tauri 窗口只通过固定 allowlist 调用 Python sidecar，
不开放本地 HTTP 端口，不把 Codex 凭据、源代码正文或原始日志放入维护产物。

## 2. 第六部分交付

### 2.1 产品级维护模块

原部署脚本中的 SQLite 维护能力已提升为可随 sidecar 打包的正式模块：

- 在线一致性备份；
- 备份完整性检查；
- 固定保留数量；
- 只按应用登记的备份 ID 恢复；
- 恢复前自动创建安全副本；
- 恢复后重新执行 Schema 初始化；
- 脱敏诊断 ZIP 导出。

备份、恢复和诊断导出都会写入系统级审计链。

### 2.2 恢复安全边界

恢复同时满足以下条件才会执行：

1. 选择的文件必须位于应用备份目录；
2. 备份 ID 必须符合固定格式并通过 SQLite 完整性检查；
3. UI 必须显示危险操作确认；
4. RPC 必须收到精确的 `RESTORE_BACKUP` 确认值；
5. 不存在活动任务或后台 Run；
6. 当前数据库先保存到 `pre-restore` 安全副本。

### 2.3 诊断导出

诊断包只包含：

- 应用、Schema、Python 和操作系统版本；
- 数据库大小与完整性结果；
- 按状态汇总的任务数量；
- 最近审计事件的类型、时间和标识。

诊断包明确排除凭据、源代码、Prompt、原始日志、Checkpoint 载荷和 SQLite 数据库。

### 2.4 Windows 本地通知

桌面端使用 Tauri Notification 插件请求系统权限。通知默认关闭，由用户在设置中
主动开启。任务进入以下状态时发送本地提醒：

- `WAITING_FOR_APPROVAL`；
- `NEEDS_ATTENTION`；
- `SUCCEEDED`。

通知只包含任务标题和下一步，不包含代码或敏感载荷。

### 2.5 完整桌面维护体验

设置页现在可以：

- 查看最近备份和保留策略；
- 创建经过完整性检查的备份；
- 经二次确认恢复最近备份；
- 导出脱敏诊断包；
- 开启或关闭 Windows 本地通知；
- 查看成功、失败和恢复引导。

成功提示只在后端操作和界面刷新都结束后出现，用户看到完成提示时可以安全继续。

## 3. 额外修复

阶段九第五部分新增的 `task/detail` 已补入 Tauri 原生 allowlist。此前 mock 和 Python
RPC 测试可以通过，但真实 Tauri 窗口会拒绝该方法；现在 Rust allowlist 测试覆盖
详情与维护方法，防止再次遗漏。

## 4. 阶段九退出标准

- [x] 完整任务流程不使用终端；
- [x] UI 不直接修改数据库；
- [x] 危险动作具有确认和审计；
- [x] 刷新、窗口关闭和后台重启不会丢失任务状态；
- [x] 主要桌面用户旅程通过自动化测试；
- [x] 本地通知由用户主动授权；
- [x] 维护产物不包含凭据、代码正文或原始日志。

## 5. 下一阶段

阶段十进入 Windows 安装与打包：

1. 生成自包含 Python sidecar；
2. 启用 Tauri NSIS 或 MSI bundling；
3. 制作开始菜单、桌面快捷方式和可选登录后启动；
4. 分离程序目录与用户数据目录；
5. 验证覆盖安装、迁移、卸载和数据保留；
6. 建立可重复、可校验的候选安装包流水线。
