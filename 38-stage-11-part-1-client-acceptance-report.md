# 阶段十一第一部分实施报告：真实客户端验收基线

## 结论

阶段十一已经启动。本部分先建立真实 Windows 客户端验收的证据边界和机器可读门禁，
不把 GitHub Actions runner、兼容模式或未执行的人工项目冒充为客户端签收。

当前开发机已识别为 Windows 11，后续可以在这里完成一轮真实交互验收。Windows 10
22H2 必须在另一台真实机器或对应虚拟机上单独执行；在取得这份证据前，完整
Windows 10/11 矩阵保持未完成。

## 已交付

- `packaging/new-windows-client-acceptance.ps1`
  - 只依赖 Windows PowerShell；
  - 绑定候选版本、完整 Git 提交、安装包文件名与 SHA-256；
  - 自动记录 Windows 版本、构建号和架构；
  - Windows 10 22H2 只接受构建 19045，Windows 11 依据构建号识别；
  - 所有检查项默认都是 `not-tested`。
- `packaging/stage11_acceptance.py`
  - 校验报告结构、必填项和状态；
  - 拒绝缺失检查、重复检查、无说明的失败或阻断项；
  - 拒绝疑似 API Key、访问令牌或 Authorization 内容；
  - 完整签收时要求每项真实通过；
  - 矩阵签收时强制要求 Windows 10 22H2 与 Windows 11 使用同一候选包。
- `docs/stage-11-client-acceptance.md`
  - 给出两台客户端的执行顺序、证据原则和最终签核命令；
  - 明确不能用 CI、兼容模式或示例数据替代真实用户流程。
- 自动化测试覆盖完整报告、未测试项、失败说明、敏感数据、候选包一致性和双系统矩阵。

## 本部分验收项目

每台客户端必须分别完成：

1. 交互式安装和首次启动；
2. 真实 Codex 登录；
3. 选择真实仓库并完成真实任务；
4. 200% 缩放和纯键盘主要流程；
5. 本地通知；
6. 卸载界面文字和默认保留数据；
7. 误报、阻断缺陷和用户反馈记录。

## 当前边界

- 本提交只建立并验证验收工具，不会预填任何人工通过结论。
- Windows 11 的安装和界面验收要绑定合并后 `main` 的成功 CI 候选安装包。
- Windows 10 22H2 尚无本机证据。
- “真实用户试用”需要由用户完成主观体验签收；自动化只能记录客观行为。

## 下一步

1. 等待合并后 `main` 的候选安装包 CI 全部通过；
2. 在当前 Windows 11 客户端下载并核对该候选包；
3. 执行交互安装、登录、真实任务、200% 缩放、键盘、通知和卸载检查；
4. 记录 Windows 11 证据和发现的缺陷；
5. 在 Windows 10 22H2 重复同一流程后关闭客户端矩阵项。
