# 阶段七：桌面用户体验规格

## 1. 体验目标

用户不使用终端即可选择代码仓库、创建任务、观察 Codex 工作、处理审批、暂停或
恢复、查看验收证据并取得最终报告。任何等待或失败页面都必须回答：

1. 现在发生了什么；
2. 为什么停在这里；
3. 系统已经安全保存了什么；
4. 用户下一步可以做什么。

## 2. 信息架构

```text
首次使用
├─ 欢迎与环境检查
├─ Codex 登录
├─ 数据与备份
└─ 启动与通知

主应用
├─ 首页
├─ 新建任务
├─ 任务详情
│  ├─ 概览
│  ├─ 活动
│  ├─ 代码变更
│  ├─ Checkpoint
│  ├─ 验收
│  └─ 报告
├─ 审批中心
└─ 设置与维护
   ├─ Codex 账户
   ├─ 启动与通知
   ├─ 数据与备份
   ├─ 日志与诊断
   └─ 关于
```

## 3. 主要用户旅程

### J1 首次启动

1. 应用检查 Windows 版本、WebView2、数据目录可写性、Git、sidecar 和数据库。
2. 用户选择“使用 ChatGPT 登录”或“使用 API Key”。
3. ChatGPT 登录打开系统浏览器；失败时可改用设备代码。
4. 应用展示默认数据与备份位置，允许修改备份目录。
5. 用户选择是否启用登录后启动，默认关闭。
6. 完成后进入首页；任何失败都保留“重试”和“导出诊断”。

### J2 创建并启动任务

1. 用户选择 Git 仓库。
2. 应用验证仓库、HEAD、分支、未提交修改和 Worktree 能力。
3. 用户填写目标，选择只读或工作区写入权限。
4. 用户配置必选/可选验收检查、超时和最大修复次数。
5. 确认页展示基准 revision、将创建的分支、隔离目录、权限和验收摘要。
6. 应用创建 Task、分支和 Worktree，校验成功后进入 `READY`。
7. 用户选择“开始”，应用取得单活动 Task 租约并启动真实 Codex Run。

### J3 后台执行与额度等待

1. 任务详情持续展示当前阶段、最近活动和下一步。
2. 用户关闭窗口后，应用隐藏到托盘，任务继续。
3. Codex 报告限额时，系统写入 Checkpoint 并展示预计恢复时间和降级回查状态。
4. 限额恢复后系统校验 Worktree，构建 Resume Package 并继续新 Run。
5. 若恢复条件不可信或 Worktree 漂移，任务进入“需要处理”，不盲目继续。

### J4 高风险审批

1. 系统暂停在 `WAITING_FOR_APPROVAL` 并发出本地通知。
2. 审批页面展示动作类型、精确参数、风险、回滚方式、动作哈希和有效期。
3. 用户批准或拒绝；参数变化或过期会使旧 Approval 无效。
4. 批准只消费一次；结果不确定的 Side Effect 进入人工对账。

### J5 暂停、退出和恢复

1. 用户选择暂停，系统请求 Codex 中断并等待安全边界。
2. 系统写入并校验 Checkpoint 后才显示“已暂停”。
3. 用户显式退出应用时执行相同安全暂停流程。
4. 重启后应用恢复持久状态，校验 Run 租约、Checkpoint 和 Worktree。
5. 用户选择恢复；系统构建 Resume Package 并开始新 Run。

### J6 完成与交付

1. Codex 完成一个阶段后自动运行 Verification Policy。
2. 可修复失败进入有限修复循环；预算耗尽进入“需要处理”。
3. 必选检查全部通过后才进入 `SUCCEEDED`。
4. 用户查看变更、验证证据和 Delivery Report。
5. 用户可批准自动提交，但应用不自动合并或推送。
6. 用户决定保留或清理干净的 Worktree。

### J7 备份与恢复

1. 用户创建备份，应用执行 SQLite 在线一致性备份并包含 Checkpoint 与必要元数据。
2. 应用验证备份清单、哈希、数据库完整性和 Schema 兼容性后才报告成功。
3. 恢复要求无活动 Task，并在覆盖前创建恢复前快照。
4. 恢复失败时回到恢复前快照；凭据永远不随备份迁移。

## 4. 低保真线框

### 4.1 首页

```text
┌ AI Agent Orchestrator ────────────────────────────────────────┐
│ 首页          审批 1          设置                      ● 已登录 │
├───────────────────────────────────────────────────────────────┤
│ 当前任务                                                     │
│ 为登录模块补充测试                         等待你的批准        │
│ ████████████░░░  自动验收：3/4                               │
│ 下一步：批准创建 Git 提交                                    │
│ [打开任务] [查看审批] [暂停]                                 │
├───────────────────────────────────────────────────────────────┤
│ 最近活动                         │ 系统状态                    │
│ 14:31 单元测试通过               │ 后台：正常                  │
│ 14:30 Codex 完成修改             │ Checkpoint：已验证          │
│ 14:24 从额度等待恢复             │ 备份：今天 02:00            │
└───────────────────────────────────────────────────────────────┘
```

无活动任务时，主体替换为“新建任务”主按钮、最近仓库和最近报告。

### 4.2 新建任务

```text
┌ 新建任务 ─────────────────────────────────────────────────────┐
│ ① 仓库  ─  ② 目标  ─  ③ 权限  ─  ④ 验收  ─  ⑤ 确认          │
├───────────────────────────────────────────────────────────────┤
│ 仓库：C:\Projects\example                           [更改]    │
│ 基准：main @ a1b2c3d                                         │
│ ⚠ 原目录有 2 个未提交修改；它们不会进入本任务                │
│                                                               │
│ 将创建：aiao/task-7f31                                       │
│ 隔离目录：...\worktrees\task_7f31                            │
│                                                               │
│ [取消]                                     [确认并继续]        │
└───────────────────────────────────────────────────────────────┘
```

### 4.3 任务详情

```text
┌ 为登录模块补充测试 ──────────────────────────── 运行中 ───────┐
│ 概览 | 活动 | 代码变更 | Checkpoint | 验收 | 报告             │
├───────────────────────────────────────────────────────────────┤
│ 当前阶段：修改实现                                             │
│ Codex 正在编辑 src/auth.py                                    │
│ 最近安全保存：2 分钟前                         [暂停] [取消]   │
│                                                               │
│ 时间线                                                        │
│ ✓ 仓库与权限已验证                                            │
│ ✓ Worktree 已创建                                             │
│ ● Codex 第 2 次运行                                           │
│ ○ 自动验收                                                    │
│ ○ 最终报告                                                    │
└───────────────────────────────────────────────────────────────┘
```

### 4.4 审批

```text
┌ 需要你的批准 ─────────────────────────────────────────────────┐
│ 动作：创建 Git 提交                                           │
│ 参数：仓库 task_7f31；包含 6 个文件；提交说明 “Add tests”     │
│ 风险：将写入本地 Git 历史                                     │
│ 回滚：可在未推送前删除任务分支上的提交                         │
│ 有效期：还剩 12 分钟                                          │
│                                                               │
│ 参数摘要哈希：9e4c…a21f                                      │
│ [拒绝]                                           [批准一次]    │
└───────────────────────────────────────────────────────────────┘
```

### 4.5 需要处理

```text
┌ 任务需要处理 ─────────────────────────────────────────────────┐
│ 恢复前发现 Worktree 与上次 Checkpoint 不一致                   │
│                                                               │
│ 已保护：原仓库未被修改；任务未继续；Checkpoint 保持有效        │
│ 发现：2 个外部修改                                             │
│ 下一步：[查看差异] [接受当前状态并重新建立恢复点] [保持暂停]   │
│                                                               │
│ 技术详情 ▸                                                    │
└───────────────────────────────────────────────────────────────┘
```

## 5. UI 动作与应用服务映射

| UI 动作 | 目标调用 | 规则 |
| --- | --- | --- |
| 完成首次检查 | `system/status` | 只读，不写 Task 状态 |
| 登录或退出 Codex | `CodexSessionService` | 凭据仅由 Codex/keyring 持有 |
| 选择仓库 | `WorktreeService.inspect_repository` | 只读预检 |
| 创建任务 | `TaskLifecycleService.create` | 原子记录 Task 与 Worktree 意图 |
| 开始 | `TaskLifecycleService.start` | 校验 `expectedVersion` 和单活动互斥 |
| 暂停 | `TaskLifecycleService.pause` | 中断完成且 Checkpoint 有效后才确认 |
| 恢复 | `TaskLifecycleService.resume` | 验证 Checkpoint 与 Worktree 后发事件 |
| 取消 | `TaskLifecycleService.cancel` | 先停止 Run，再进入 `CANCELLED` |
| 批准或拒绝 | `ApprovalService.decide` | 动作哈希、有效期和一次性消费 |
| 查看日志或 Diff | `DesktopQueryService` | 分页、脱敏、受控路径 |
| 自动提交 | `SideEffectCoordinator.execute` | 必须有精确 Approval |
| 清理 Worktree | `WorktreeService.cleanup` | 终态、干净、显式确认 |
| 创建或恢复备份 | `BackupService` | 恢复时要求无活动 Task |
| 更改启动设置 | `SettingsService` 经 Tauri | 默认关闭并审计设置变化 |
| 导出诊断 | `DesktopQueryService.export_diagnostics` | 默认脱敏，不含代码和凭据 |

## 6. 状态呈现

| 内部状态 | 默认用户文案 | 主要动作 |
| --- | --- | --- |
| `DRAFT` | 设置任务 | 继续配置、删除草稿 |
| `READY` | 可以开始 / 可以继续 | 开始、取消 |
| `RUNNING` | Codex 正在工作 | 暂停、取消、隐藏到后台 |
| `PAUSED`（阶段八新增） | 已安全暂停 | 恢复、取消 |
| `WAITING_FOR_SIGNAL` | 正在等待外部条件 | 查看原因、取消 |
| `WAITING_FOR_APPROVAL` | 等待你的批准 | 查看审批、拒绝 |
| `VERIFYING` | 正在自动验收 | 查看检查、取消 |
| `NEEDS_ATTENTION` | 需要处理 | 查看原因、按建议修复 |
| `SUCCEEDED` | 已完成并通过验收 | 查看报告、批准提交 |
| `CANCELLED` | 已取消 | 查看保留内容、清理 |

## 7. 可访问性与错误设计

- 所有功能可用键盘完成，焦点顺序与视觉顺序一致。
- 状态不能只用颜色表达；同时使用文字与图标。
- 正文和控件达到 WCAG 2.2 AA 对比度目标。
- 动画尊重 Windows“减少动画”设置。
- 流式日志更新不抢夺焦点，并允许暂停自动滚动。
- 错误使用稳定错误码、用户说明、已保护内容和下一步操作四部分。
- API Key 输入默认遮蔽，不提供复制回显，提交后立即从 UI 内存清除。
- 危险按钮使用动作名称，不使用含糊的“确定”。

## 8. v1 UI 验收场景

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| UI-01 | 干净 Windows 首次启动 | 无终端完成检查、登录和数据初始化 |
| UI-02 | 浏览器登录失败 | 可切换设备代码并成功恢复向导 |
| UI-03 | 选择有未提交修改的仓库 | 明示修改不纳入，且不 stash 或改写原仓库 |
| UI-04 | 创建 workspace-write Task | 确认页准确显示权限、分支、Worktree 和验收 |
| UI-05 | 同时已有活动 Task | 阻止启动第二个并提供打开当前 Task 的入口 |
| UI-06 | 关闭窗口 | 窗口隐藏、托盘可见、Run 继续且首次有解释 |
| UI-07 | 安全暂停 | Codex 中断并验证 Checkpoint 后才显示已暂停 |
| UI-08 | 强制结束后重启 | 识别过期租约，未重复执行，提供安全恢复 |
| UI-09 | 额度耗尽与恢复 | 展示等待原因和时间，恢复后校验并继续 |
| UI-10 | Approval 参数变化 | 旧 Approval 失效，不能执行变化后的动作 |
| UI-11 | Side Effect 结果未知 | 不自动重放，进入人工对账 |
| UI-12 | 验收失败可修复 | 在预算内修复，展示每次证据 |
| UI-13 | 验收预算耗尽 | 不显示成功，进入需要处理 |
| UI-14 | 成功交付 | 必选检查全通过并可查看报告与 Diff |
| UI-15 | 自动提交 | 精确审批后只提交 Task 分支，不推送或合并 |
| UI-16 | 脏 Worktree 清理 | 清理被阻止，文件保持不变 |
| UI-17 | 在线备份和恢复 | 校验成功；恢复前快照存在；凭据未进入备份 |
| UI-18 | 升级迁移失败 | 旧版本与备份可恢复，半迁移数据库不启动 |
| UI-19 | 卸载默认选项 | 用户数据、Worktree 和备份全部保留 |
| UI-20 | 诊断导出 | 无令牌、原始认证、代码正文或未脱敏敏感值 |
| UI-21 | 键盘与缩放 | 仅键盘和 200% 缩放可完成主流程 |

## 9. 阶段九实现门槛

- 先以 Fake Adapter 完成 UI-01、UI-03 至 UI-08、UI-10 至 UI-14。
- 再以阶段八真实 Codex 主链完成 UI-02、UI-09 和 UI-15。
- 安装器环境完成后执行 UI-17 至 UI-19。
- 每个危险动作的自动化测试必须同时验证审计记录。
- 视觉实现可以调整布局，但不得改变本规格中的领域边界和用户安全语义。
