# 产品化路线图：从技术 MVP 到可安装桌面产品

## 1. 产品目标

最终产品不是只能由开发者操作的命令行工具，而是一个可安装的 Windows 本地应用：

> 用户安装应用，完成 Codex 登录，选择代码项目并输入任务；系统自动执行、暂停、恢复和验收；用户在图形界面中完成高风险审批并查看进度、日志和最终报告。

底层编排器、状态机和 SQLite 是产品内核。桌面界面、真实执行主链、安装升级和用户验收同样属于产品完成条件。

## 2. v1.0 完成定义

v1.0 必须同时满足：

- 在干净的受支持 Windows 环境中通过一个安装程序完成安装；
- 用户不需要预先安装 Python、Docker 或使用终端；
- 提供正常的 Windows 桌面应用窗口；
- 首次启动可以完成 Codex 登录和本地数据目录初始化；
- 用户可以选择 Git 仓库、填写任务、权限、验收规则和重试预算；
- 可以启动真实 Codex 执行，并在界面中暂停、恢复和取消；
- 额度等待、进程中断和应用重启后可以从持久状态恢复；
- 高风险动作在界面中显示精确参数、风险和回滚说明并等待审批；
- 必选验收全部通过前不能显示成功；
- 可以查看任务状态、运行记录、检查点、日志摘要和交付报告；
- 可以执行和验证本地备份与恢复；
- 升级和卸载默认保留用户数据；
- 默认不收集遥测，不开放公网端口，不创建云资源；
- 同时只允许一个活动任务。

## 3. v1.0 纳入范围

| 编号 | 功能 | 决策 |
| --- | --- | --- |
| V1-01 | Windows 本地桌面 UI | 纳入 |
| V1-02 | `.exe` 或 `.msi` 安装包 | 纳入 |
| V1-03 | 真实 Codex 自动执行入口 | 纳入 |
| V1-04 | 首次启动与 Codex 登录向导 | 纳入 |
| V1-05 | 项目选择和任务创建界面 | 纳入 |
| V1-06 | 开始、暂停、恢复和取消 | 纳入 |
| V1-07 | 高风险操作审批界面 | 纳入 |
| V1-08 | 日志、进度、检查点和最终报告 | 纳入 |
| V1-09 | 本地备份与恢复界面 | 纳入 |
| V1-10 | 后台运行和可选登录后启动 | 纳入 |
| V1-11 | 安装、升级和卸载时保护数据 | 纳入 |
| V1-12 | Git worktree 任务隔离 | 纳入 |
| V1-13 | 自动提交 Git 变更 | 支持，默认审批 |
| V1-14 | 本地系统通知 | 纳入 |

## 4. 已批准的产品决策

| 决策 | 结果 |
| --- | --- |
| 界面形式 | 桌面应用窗口 + 本地后台服务 |
| v1.0 更新方式 | 手动更新，v1.1 再评估自动更新 |
| GitHub Webhook | v1.0 默认不开放公网 |
| 自动提交 | 支持，但默认弹窗审批 |
| 登录后启动 | 安装时可选，默认关闭 |
| 遥测 | v1.0 完全不收集 |
| 卸载数据 | 默认保留，显式选择后才删除 |
| 任务范围 | 单用户、单机、同时一个活动任务 |
| 后端语言 | Python |
| 状态存储 | SQLite |
| Checkpoint | JSON |
| 首个执行引擎 | Codex |
| 云端部署 | 当前不做 |
| Docker | 保留为可选发布能力，不是安装前提 |

## 5. 实施阶段

### 阶段 7：产品定义与桌面架构（已完成）

目标：把技术组件收敛成可实现、可测试的桌面产品设计。

交付：

- 用户旅程、信息架构和主要界面线框；
- 桌面壳、本地 API、后台进程和 SQLite 的进程边界；
- Codex 登录与凭据边界；
- 安装目录、用户数据目录、日志目录和备份目录规范；
- Git worktree 生命周期和仓库安全模型；
- 本地 API 权限、会话和防跨站请求设计；
- 安装、升级、卸载和数据迁移契约；
- v1.0 UI 验收场景。

退出标准：

- [x] 用户无需终端即可完成完整任务流程；
- [x] 每个界面动作都映射到现有领域命令或一个明确的新应用服务；
- [x] UI 不绕过状态机、审批、幂等账本或敏感信息过滤；
- [x] 选定桌面技术栈和 Windows 打包方案；
- [x] 通过一次正式架构评审。

设计与评审证据见
[24-stage-7-product-architecture.md](./24-stage-7-product-architecture.md)、
[25-stage-7-user-experience-spec.md](./25-stage-7-user-experience-spec.md) 和
[26-stage-7-completion-report.md](./26-stage-7-completion-report.md)。

### 阶段 8：真实 Codex 执行主链（已完成）

目标：把现有 Codex Adapter 变成产品可调用的持久后台执行能力。

交付：

- 本地运行环境安装和验证 Codex SDK；
- Codex 登录状态查询和失效恢复；
- `READY` 任务安全分派；
- 启动、收集、暂停、恢复和取消应用服务；
- Run 心跳、租约和后台进程重启恢复；
- 额度等待和低频回查；
- 自动验收、有限修复和最终报告闭环；
- 单活动任务互斥；
- Git worktree 创建、校验和清理。

退出标准：

- [x] 从一个应用服务调用完成真实代码任务；
- [x] 强制中断应用后可以恢复；
- [x] 工作区漂移和审批仍能阻止不安全执行；
- [x] 不需要人工发出底层状态机事件；
- [x] 通过真实 Codex 端到端验收。

实现与验收证据见 [27-stage-8-implementation-report.md](./27-stage-8-implementation-report.md)。

### 阶段 9：桌面 UI

目标：普通用户可以在图形界面中完成所有 v1.0 核心操作。

当前进度：第一部分已完成 React/TypeScript 界面基础、Fake Adapter 用户旅程、
只读私有 RPC 和前端 CI；原生 Tauri 窗口、真实写操作与通知仍在后续部分。

交付：

- 首次启动与登录；
- 项目选择、最近项目和仓库校验；
- 任务创建与权限配置；
- 任务列表、状态、进度和下一步；
- 暂停、恢复、取消；
- 审批中心；
- 日志、检查点、验收证据和交付报告；
- 服务状态、备份恢复和设置；
- Windows 本地通知；
- 可访问性、空状态、错误和恢复引导。

退出标准：

- [ ] 完整任务流程不使用终端；
- [ ] UI 不直接修改数据库；
- [ ] 所有危险动作都有二次确认和审计；
- [ ] 刷新、关闭窗口和后台重启不会丢失任务状态；
- [ ] 通过主要用户旅程自动化测试。

### 阶段 10：Windows 安装与打包

目标：用户通过一个安装程序获得可运行产品。

交付：

- 自包含 Windows 应用构建；
- 安装程序、开始菜单和可选桌面快捷方式；
- 安装时可选登录后启动；
- 用户数据与程序文件分离；
- 手动升级与 Schema 迁移；
- 卸载时默认保留数据；
- 明确的版本信息和诊断导出；
- 可重复、可校验的发布流水线。

退出标准：

- [ ] 干净 Windows 环境无需 Python 或 Docker 即可安装；
- [ ] 安装后首次启动成功；
- [ ] 覆盖安装不丢失任务和备份；
- [ ] 卸载默认保留数据；
- [ ] 安装包通过恶意软件扫描和来源校验；
- [ ] 产生候选版本安装包。

### 阶段 11：产品验收与 v1.0 发布

目标：证明产品可以交给真实用户，而不仅是开发环境可运行。

交付：

- 多个真实仓库和任务类型测试；
- 新装、升级、卸载和重装矩阵；
- 应用崩溃、电脑重启、Codex 退出和额度等待演练；
- 备份恢复和数据迁移演练；
- UI 错误处理、可访问性和性能检查；
- 安全、隐私和依赖审查；
- 用户手册、故障排查和已知限制；
- v1.0 发布候选版和最终版。

退出标准：

- [ ] 全部 v1.0 完成定义有可审计证据；
- [ ] 阻断级缺陷清零；
- [ ] 至少完成一轮真实用户试用和反馈修复；
- [ ] 发布、回滚和数据恢复流程均已演练；
- [ ] 发布 v1.0。

## 6. 延期到 v1.x

| 编号 | 功能 | 目标 |
| --- | --- | --- |
| D-01 | 多任务并发调度 | v1.1 |
| D-02 | 多仓库任务 | v1.1 |
| D-03 | 大型日志压缩与归档 | v1.1 |
| D-04 | GitHub 固定 Webhook 或安全隧道 | v1.1，可选 |
| D-05 | 自动更新 | v1.1 |
| D-06 | Windows 代码签名 | v1.1 或首次公开发布前 |
| D-07 | 用户同意后的崩溃报告 | v1.1，默认关闭 |
| D-08 | 邮件、Slack 等外部通知 | v1.2 |

## 7. 长期延期

| 编号 | 功能 | 目标 |
| --- | --- | --- |
| L-01 | 多 Agent 协作 | v2.0 后 |
| L-02 | 云端 Web 控制台 | v2.0 后 |
| L-03 | GitHub App 深度集成 | v2.0 后 |
| L-04 | 语义检索与长期记忆 | v2.0 后 |
| L-05 | 多模型和自适应路由 | v2.0 后 |
| L-06 | 成本优化与预算自动分配 | v2.0 后 |
| L-07 | 团队、多用户和权限体系 | v2.0 后 |
| L-08 | 自动发现所有供应商的额度恢复时间 | 长期研究 |

延期项不得在没有更新本文件和验收范围的情况下悄悄进入 v1.0。
