# 阶段 6 第六部分：Railway 生产托管准备

## 1. 结论

MVP 的首个长期托管平台选择 Railway。它可以从公开 GHCR 镜像部署服务，并在同一项目内提供公开 HTTPS 域名、持久卷、SQLite 卷备份、运行变量和用量控制。

本部分不创建付费资源，也不写入生产密钥。合并后先发布 `v0.8.0`，取得并验证新镜像摘要；随后由仓库所有者登录 Railway，审阅套餐和费用上限，再按 [Railway 生产部署手册](./deploy/railway.md) 创建资源。

## 2. 平台比较

| 平台 | 适配结论 |
| --- | --- |
| Railway | 首选。MVP 所需的镜像部署、稳定 HTTPS、单实例卷、SQLite 备份和费用限制均有直接支持，操作路径较短。 |
| Fly.io | 备选。卷、快照、Secrets 和证书能力完整，但机器、网络和卷的运维面更大。 |
| Render | 备选。Docker、HTTPS 和持久盘可用，但持久盘要求付费服务，挂载布局限制更多。 |
| DigitalOcean App Platform | 暂不选择。当前 SQLite 单文件持久化不是最直接的部署路径。 |

选择只适用于当前单实例 SQLite MVP。若未来需要横向扩展、高可用或多区域写入，应先把状态迁移到独立数据库，再重新评估平台。

## 3. 发现并修复的部署阻塞

Railway 持久卷默认以 root 所有者挂载。原镜像在容器元数据中固定使用 `orchestrator`，因此新卷上的 `/data/state.db` 可能因无写权限而启动失败。

新增的容器入口遵循最小范围：

1. 仅当入口本身以 root 启动时执行准备；
2. 拒绝把 `/data` 符号链接或普通文件当作数据目录；
3. 只修改 `/data` 顶层目录的 UID/GID；
4. 初始化应用用户的组；
5. 切换到 UID/GID `10001`；
6. 再执行原始容器命令。

常规 Docker 和 Compose 仍以镜像的 `orchestrator` 用户启动，不经过 root 准备。Railway 通过 `RAILWAY_RUN_UID=0` 允许入口准备 root 所有的新卷，但应用命令最终仍是 UID `10001`。

## 4. 发布门禁

发布工作流不再只读取镜像的 `Config.User`，而是执行以下运行时验证：

- 默认启动执行 `id -un`，结果必须是 `orchestrator`；
- 创建一个新的 Docker 卷；
- 模拟 Railway 以 UID 0 启动；
- 向卷写入文件；
- 从 `/proc/self/status` 验证实际命令 UID 是 `10001`。

因此 `v0.8.0` 在发布前会同时证明“卷可写”和“应用已降权”。

## 5. 生产不变量

- 只部署通过来源验证的不可变 `sha256` 摘要；
- 只有一个副本；
- 卷只挂载到 `/data`；
- 数据库固定为 `/data/state.db`；
- 不覆盖镜像 `ENTRYPOINT`；
- webhook 密钥至少 32 字符并在 Railway 中封存；
- 健康检查使用 `/readyz`；
- 域名 target port 固定为 `8080`；
- 创建资源前确认套餐和 hard limit；
- 启用日、周、月备份并完成一次恢复演练。

## 6. 下一步

1. 审核并合并本 PR；
2. 等待主分支 CI 通过；
3. 创建注释标签 `v0.8.0`；
4. 验证 GHCR 摘要、运行时降权和来源证明；
5. 登录 Railway 确认费用并创建生产资源；
6. 配置真实 GitHub Webhook；
7. 执行生产端到端验收和备份恢复演练。
