# 02 — MVP 需求

## 1. MVP 要回答的问题

> 当一次本地开发任务因可恢复原因暂停后，系统能否在没有用户重新描述上下文的情况下，安全地恢复执行并给出可验证结果？

若不能证明这一点，多 Agent、云端控制台和复杂调度都没有必要。

这里的 MVP 是“技术内核 MVP”，不是最终可分发产品。技术闭环验证完成后，项目继续交付可安装 Windows 桌面应用；产品完成定义和阶段 7–11 见 [23-productization-roadmap.md](./23-productization-roadmap.md)。

## 2. 范围

第一版只支持：

- 单用户；
- 单机；
- 单个代码仓库；
- 同时一个活动任务；
- 一个执行引擎适配器；
- 本地持久化；
- 定时器、人工操作和命令结果三类事件；
- 基于命令的自动验收；
- 高风险动作人工审批。

## 3. 功能需求

### FR-01 创建任务

用户可以提供：

- 任务目标；
- 工作目录；
- 允许和禁止的动作；
- 验收命令；
- 最大重试次数；
- 是否允许自动提交。

系统生成稳定的 `task_id` 并写入持久存储。

### FR-02 执行与记录

系统启动执行引擎，并记录：

- 每次运行的 `run_id`；
- 输入摘要；
- 开始与结束时间；
- 退出原因；
- 产物和验证结果；
- 下一步建议。

### FR-03 生成检查点

在暂停、失败、等待或达到阶段性目标时，系统必须写入检查点。检查点格式见 [05-checkpoint-and-resume.md](./05-checkpoint-and-resume.md)。

### FR-04 等待恢复信号

MVP 支持：

1. 到达指定时间；
2. 用户点击或输入“恢复”；
3. 某条本地检查命令成功或失败。

信号必须包含唯一标识，重复信号只能产生一次有效状态转换。

### FR-05 恢复任务

恢复前系统必须：

- 加载最新有效检查点；
- 检查工作目录是否存在；
- 获取当前版本控制状态；
- 检查关键文件是否与检查点冲突；
- 重新确认权限和剩余重试预算；
- 构造 Resume Package。

### FR-06 自动验收

任务可配置一组顺序执行的验收命令，例如：

1. 格式或静态检查；
2. 单元测试；
3. 构建；
4. 变更摘要。

每条命令记录退出状态和输出摘要。只有必选命令全部通过，任务才可进入 `SUCCEEDED`。

### FR-07 人工审批

以下动作默认暂停并请求用户批准：

- 发布或部署；
- 合并分支或 PR；
- 删除重要文件或数据；
- 修改生产环境；
- 产生费用的外部操作；
- 向第三方发送消息；
- 超出初始授权范围的动作。

### FR-08 审计日志

系统以追加方式记录：

- 状态转换；
- 收到的事件；
- 执行动作；
- 审批决定；
- 验收结果；
- 异常与重试。

## 4. 非功能需求

### NFR-01 可恢复性

进程被终止后，重新启动应能从持久状态识别当前任务，不依赖内存。

### NFR-02 幂等性

相同恢复事件、重复启动或网络重试不得导致同一外部副作用执行两次。

### NFR-03 可解释性

每次状态变化必须能回答：

- 为什么变化；
- 根据哪个事件；
- 执行了什么；
- 下一步是什么。

### NFR-04 最小权限

执行引擎仅能获得当前步骤所需权限。敏感凭据不写入提示词、检查点或普通日志。

### NFR-05 可替换性

任务状态、恢复协议和验收逻辑不能与某一个模型供应商的数据格式强绑定。

## 5. MVP 验收场景

### AC-01 正常暂停和恢复

1. 创建一个修改示例仓库的任务；
2. 执行完成第一阶段后主动暂停；
3. 终止并重启编排器；
4. 发出恢复事件；
5. 新运行读取检查点并完成剩余工作；
6. 自动测试通过；
7. 任务进入 `SUCCEEDED`。

### AC-02 重复恢复事件

同一个事件被投递两次，只能启动一次恢复运行。

### AC-03 工作区被人修改

暂停期间用户修改关键文件。恢复前系统检测到漂移，进入 `NEEDS_ATTENTION`，不得静默覆盖。

### AC-04 验收失败

测试失败时任务不得标记成功。若重试预算尚在，则生成失败检查点并继续修复；否则升级给用户。

### AC-05 高风险动作

执行引擎提出部署操作时，任务进入 `WAITING_FOR_APPROVAL`。没有批准不得执行。

## 6. 明确延后

- 多任务并发调度；
- 多 Agent 协作；
- 云端 Web 控制台；
- GitHub App 深度集成；
- 语义检索与长期记忆；
- 自适应模型路由；
- 成本优化和预算分配；
- 自动发现官方额度恢复时间。

其中本地桌面 UI、真实 Codex 产品入口和 Windows 安装包不再属于无限期延后项，已正式纳入 v1.0 产品化路线。其他延期项的目标版本见 [23-productization-roadmap.md](./23-productization-roadmap.md)。
